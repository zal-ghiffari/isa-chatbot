# Backend
DB_PATH=/data/survey.db
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173
HOST=0.0.0.0
PORT=8000

# Frontend (via nginx proxy)
# Backend service name for nginx proxy (docker-compose service name)
# BACKEND_HOST=backend
# BACKEND_PORT=8000
